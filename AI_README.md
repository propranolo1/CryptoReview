# CryptoReview 架构说明

## 项目目标

CryptoReview 是一个交易复盘工具。用户导入历史仓位与止盈止损设置后，系统读取对应交易对的历史 K 线，在图表中标注入场、离场、止盈和止损，并从入场位置逐根回放，最终计算这笔交易的已实现、未实现和总盈亏。

## 当前版本范围

- 数据源：Binance Spot 公共历史 K 线，只读取公开行情，不需要 API Key。
- 导入格式：CSV 或 JSON；支持中英文常见字段名。
- 方向：多仓与空仓。
- 图表：K 线、入场/离场标记、止盈/止损价格线。
- 回放：5 分钟、15 分钟、1 小时、4 小时、1 天；支持播放、暂停、单步、重置和速度切换。
- 结果：已实现盈亏、未实现盈亏、总盈亏、收益率、持仓数量和风险收益比。
- 存储：导入记录与复盘笔记仅保存在当前浏览器，不上传用户文件。

## 代码结构

```text
app/
  api/market/klines/route.ts  # Binance K 线同源代理与参数校验
  components/TradeReplay.tsx # 导入、交易列表、图表、回放与结果界面
  page.tsx                    # 单页入口
  layout.tsx                  # 全局元数据
  globals.css                 # 全局视觉与响应式样式
lib/
  trade.mjs                   # 导入解析与盈亏纯函数
  trade.d.mts                 # 领域函数的 TypeScript 声明
tests/
  trade.test.mjs              # 领域规则回归测试
```

## 数据流

1. CSV/JSON 文件在浏览器内读取，由 `parseTrades` 转换为统一交易结构。
2. 页面根据交易对、入场时间和时间框架请求 `/api/market/klines`。
3. 服务端代理校验参数后读取 Binance 公共 K 线，并转换为图表需要的 OHLCV 结构。
4. 图表初始只显示到入场 K 线；回放时逐根追加，离场标记只在时间到达后出现，避免未来数据泄漏。
5. `calculateTradePnl` 只使用回放时刻已经发生的离场成交和当前 K 线收盘价计算结果。

## 关键边界

- 所有时间统一为 ISO 字符串或 Unix 毫秒输入，图表内部统一为 Unix 秒。
- Binance 单次最多返回 1000 根 K 线；跨度过长时，细周期可能暂时无法覆盖最终离场点。
- 上游不可用、地区限制或网络失败时，页面会切换到明确标识的演示行情，导入数据和盈亏规则仍可操作。
- 首版的止盈/止损为每笔交易各一个价格水平；领域结构保留扩展多次调整历史的空间。
- 本项目只用于交易复盘，不构成投资建议，也不执行任何下单操作。

## 修改约定

- 新功能应优先新增纯函数测试，避免回放与盈亏规则被界面改动破坏。
- 行情适配、领域计算和界面状态保持分层，接入其他交易所时不修改盈亏核心。
- 不在无关需求中重构或优化现有代码；任何额外优化先提出并获得确认。
